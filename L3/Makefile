CC = gcc
CFLAGS = -Wall -pedantic -std=c99
INCLUDES = -Iinclude
BINDIR = bin/
SRCDIR = src/
SOURCE = $(SRCDIR)linkedlist.c $(SRCDIR)stack.c $(SRCDIR)testfile.c
PROGNAME = $(BINDIR)l3run

all:
	$(CC) $(CFLAG) $(SOURCE) $(INCLUDES) -o $(PROGNAME)


